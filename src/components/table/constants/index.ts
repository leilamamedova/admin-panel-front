import { ITableCredentials } from "components/table/interfaces";

export const tableInitialCredentials: ITableCredentials = Object.freeze({
  fieldSet: null,
  page: 0,
  limit: 10,
  totalCount: 0,
});
